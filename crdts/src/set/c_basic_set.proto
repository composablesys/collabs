message CBasicSetCreateMessage {
  required uint32 replicaUniqueNumber = 1;
  required bytes args = 2;
}

message CBasicSetMessage {
  oneof op {
    CBasicSetCreateMessage add = 1;
    string delete = 2;
  }
}

message CBasicSetSave {
  // In order by time created on this replica.
  // OPT: deduplicate senders.
  repeated string names = 1;
  // Aligned with names.
  repeated bytes args = 2;
}
