message PresenceMessage {
  // MessageType enum
  required int32 type = 1;
  optional bytes updates = 2;
  optional bool requestAll = 3 [default=false];
  optional bool forRequestAll = 4 [default=false];
}