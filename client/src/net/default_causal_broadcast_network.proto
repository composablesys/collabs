message DefaultCausalBroadcastMessage {
    required bytes message = 1;
    required string sender = 2;
    required uint32 senderCounter = 3;
    map<string, uint32> vectorMap = 4;
    required double time = 5;
}
