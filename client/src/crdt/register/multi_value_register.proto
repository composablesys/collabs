message MvrMessage {
    oneof data {
      bytes value = 1;
      bool reset = 2;
    }
}

message MvrMetaSave {
  required bytes value = 1;
  required string sender = 2;
  required uint32 senderCounter = 3;
  required double time = 4;
}

message MvrSave {
  repeated MvrMetaSave elements = 1;
}
