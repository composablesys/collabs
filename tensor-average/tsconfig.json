{
  "compilerOptions": {
    /* Use . as rootDir so build contains a nested src folder.
     * This keeps things consistent across different projects,
     * some of which have a "generated" folder (thus causing
     * Typescript to set rootDir = . by default) while
     * others don't.
     */
    "rootDir": ".",
    "outDir": "build",
    "target": "es2019",
    /* Specify lib manually so we can exclude DOM. */
    "lib": ["es2019"],
    /* Enable strict type checking. */
    "strict": true,
    /*
     * Necessary for TensorFlow.js.
     * See https://www.tensorflow.org/js/tutorials/setup#typescript
     */
    "skipLibCheck": true,
    "module": "commonjs",
    /* Enable interop with dependencies using different module systems. */
    "esModuleInterop": true,
    /* Include .js files in generated folder. */
    "allowJs": true,
    /* Emit .d.ts files. */
    "declaration": true,
    /* Emit sourcemap files. */
    "sourceMap": true,
    /* Use tslib to reduce generated js file sizes. */
    "importHelpers": true
  },
  "include": ["src", "generated"],
  /* ts-node gets confused by workspaces + allowJs=true
   * and needs some help to run the tests without error.
   * This fix comes from
   * https://github.com/TypeStrong/ts-node/issues/693#issuecomment-889531876
   * A detailed discussion of what's going wrong starts here:
   * https://github.com/TypeStrong/ts-node/issues/693#issuecomment-828046250
   */
  "ts-node": {
    "transpileOnly": true
  }
}
