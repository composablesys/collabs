{
  "compilerOptions": {
    "target": "ES2021",
    /* Recommend by Webpack. ES2020 so TypeScript won't mangle dynamic imports. */
    "module": "ES2020",
    /* Needed with module: ES6 or else compilation breaks. */
    "moduleResolution": "node",
    /* Enable strict type checking. */
    "strict": true,
    /* Prevent errors caused by other libraries (e.g. the DOM types). */
    "skipLibCheck": true,
    /* Enable interop with dependencies using different module systems. */
    "esModuleInterop": true,
    /* We don't need to emit declarations. */
    "declaration": false,
    /* Emit sourcemap files. */
    "sourceMap": true
  },
  "include": ["src"]
}
