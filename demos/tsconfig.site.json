{
  "compilerOptions": {
    "target": "es2020",
    /* Recommend by Webpack. */
    "module": "es2020",
    /* Needed with module: es2020 or else stuff breaks. */
    "moduleResolution": "node",
    /* Enable strict type checking. */
    "strict": true,
    /* The workspace structure causes tsc to check all
     * projects in node_modules during compilation, including
     * one tfjs dependency (webgl pinned version) that has
     * a bug.  So while we are using workspaces, we need
     * to skipLibChecks.
     */
    "skipLibCheck": true,
    "module": "commonjs",
    /* Enable interop with dependencies using different module systems. */
    "esModuleInterop": true,
    /* Include generated .js files in compilation run */
    "allowJs": true,
    /* We don't need to emit declarations. */
    "declaration": false,
    /* Emit sourcemap files. */
    "sourceMap": true,
    /* Use tslib to reduce generated js file sizes. */
    "importHelpers": true
  },
  "include": ["src/site", "generated/site"]
}
