message ImplicitMergingMutCMapSave {
  // TODO: load in creation order (like in DeletingMutCSet),
  // in case keys deserialize other values, to avoid too-deep
  // recursion?
  map<string, bytes> childSaves = 1;
}
