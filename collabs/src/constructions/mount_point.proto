message CMountPointQueuedMessage {
  repeated string targetPath = 1;
  optional bytes meta = 2;
  required bytes message = 3;
}

message CMountPointSave {
  repeated CMountPointQueuedMessage messageQueue = 1;
}
